version: 1
services:
  - type: web
    name: swarm-intelligence
    runtime: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    healthCheckPath: "/"
    envVars:
      - key: NODE_ENV
        value: production
    domains:
      - swarm-intelligence.onrender.com